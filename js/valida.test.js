import Valida from './valida';

jest.mock('./valida');

const cpf_valido = '94896402200';
const cpf_invalido = '12312312300';

test('valida cpf ', () => {
  // const isValid = Valida.get
  // expect(Valida.valida_cpf(cpf_valido)).toBeTruthy();
})